<%# index for posts, targets turbo_post frame %>

<% @posts.each do |post| %>
    <div>
        <h2><%= link_to post.title, post_path(post), data: { turbo_frame: "turbo_post" } %></h2>
        <p><%= post.author.full_name %></p>
    </div>
<% end %>
